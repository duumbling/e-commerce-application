export interface CustomerData {
  firstName?: string;
  lastName?: string;
  dateOfBirth?: string;
  email?: string;
}
export interface CustomerAddressData {
  city: string;
  country: string;
  id: string;
  postalCode: string;
  streetName: string;
}
